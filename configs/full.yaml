name: structured-memory-hybrid
seed: 42
retrieval_top_k: 100
enable_memory: true
enable_semantic_retrieval: true
structured_planning: true
# Frozen after validation-only grid search (step=0.1, 500 users).
weights: [0.7, 0.3, 0.0]
